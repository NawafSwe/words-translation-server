/* ------------ Requiring Packages ------------ */
const User = require('../models/user');

const postUser = async (body) => {
    try {

    } catch (error) {
        console.log(`error occurred during adding new user ${error}`);
    }

}
